[workspace]
resolver = "2"
members = ["core", "gui", "tui"]

[workspace.package]
version = "0.1.0"
edition = "2021"

# Masaustu (Windows) ve TUI icin ortak profil
[profile.release]
opt-level = 3
lto = "thin"
strip = true
